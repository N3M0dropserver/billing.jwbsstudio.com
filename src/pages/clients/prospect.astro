---
/**
 * The old prospecting page.
 *
 * Superseded by the growth pipeline, which does what this promised and never
 * did. Kept as a redirect rather than deleted so a bookmark still lands
 * somewhere useful.
 */
return Astro.redirect('/growth', 301);
---
